import { betweenDates, errorDates, getDate, leftDates, validateDates } from "../src/client/js/validateDates";


describe("Testing the URL Check functionality", () => {
    test("Testing the checkUrl() function", () => {
           expect(validateDates).toBeDefined();
})});

describe("Testing the URL Check functionality", () => {
    test("Testing the checkUrl() function", () => {
           expect(getDate).toBeDefined();
})});

describe("Testing the URL Check functionality", () => {
    test("Testing the checkUrl() function", () => {
           expect(errorDates).toBeDefined();
})});

describe("Testing the URL Check functionality", () => {
    test("Testing the checkUrl() function", () => {
           expect(leftDates).toBeDefined();
})});

describe("Testing the URL Check functionality", () => {
    test("Testing the checkUrl() function", () => {
           expect(betweenDates).toBeDefined();
})});